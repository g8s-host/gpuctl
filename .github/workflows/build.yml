name: Build Linux Binary

on:
  push:
    branches: [ main ]
    tags: [ 'v*' ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-22.04
    strategy:
      matrix:
        python-version: [3.12]

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install poetry
        poetry install
        pip install pyinstaller

    - name: Build binary with PyInstaller
      run: |
        pyinstaller --onefile --name=gpuctl main.py

    - name: Verify binary
      run: |
        chmod +x dist/gpuctl
        ./dist/gpuctl --help

    - name: Upload binary as artifact
      uses: actions/upload-artifact@v4
      with:
        name: gpuctl-linux-amd64
        path: dist/gpuctl

    - name: Create Release
      if: startsWith(github.ref, 'refs/tags/')
      uses: softprops/action-gh-release@v2
      with:
        files: dist/gpuctl
        name: Release ${{ github.ref_name }}
        draft: false
        prerelease: false
        body: |
          ## 🚀 gpuctl ${{ github.ref_name }} 发布
          
          ### 可执行文件
          - [gpuctl](https://github.com/${{ github.repository }}/releases/download/${{ github.ref_name }}/gpuctl) - Linux 64位二进制包
          
          ### 使用方式
          ```bash
          wget https://github.com/${{ github.repository }}/releases/download/${{ github.ref_name }}/gpuctl -O gpuctl
          chmod +x gpuctl
          sudo mv gpuctl /usr/local/bin/
          gpuctl --help
          ```
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}