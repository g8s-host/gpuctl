# K8s集群配置
# 请将此文件复制为.env并填写真实配置

# ==========================
# 本地开发环境配置（默认）
# ==========================
# 本地开发环境下，以下配置可以留空，自动使用本地kubeconfig文件
# 仅当需要连接远程K8s集群时才需要填写以下配置

# K8s集群master节点IP（远程集群必填，本地集群可选）
# K8S_MASTER_IP=192.168.1.100

# K8s API端口，默认6443（远程集群必填，本地集群可选）
# K8S_API_PORT=6443

# 访问K8s集群的令牌（远程集群必填，本地集群可选）
# K8S_TOKEN=eyJhbGciOiJSUzI1NiIsImtpZCI6Ik...

# CA证书路径（可选）
# K8S_CA_CERT=/path/to/ca.crt

# ==========================
# 通用配置
# ==========================

# 日志级别：DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=DEBUG

# API服务器配置
API_HOST=0.0.0.0
API_PORT=8000

# 默认命名空间（可选，默认使用g8s-system）
# DEFAULT_NAMESPACE=g8s-system

